*** Begin Patch
*** Update File: includes/PanelHelper.php
@@
-        // 存储类型筛选
-        // WebDAV 文件在上传时会在 text 字段中添加 'storage' => 'webdav' 标记
-        if ( !== 'all') {
-            if ( === 'webdav') {
-                // 筛选 WebDAV 文件：查找 text 字段包含 webdav 存储标记的文件
-                ->where('table.contents.text LIKE ?', '%s:7:"storage";s:6:"webdav"%');
-            } elseif ( === 'local') {
-                // 筛选本地文件：排除带有 webdav 标记的文件
-                ->where('table.contents.text NOT LIKE ?', '%s:7:"storage";s:6:"webdav"%');
-            }
-        }
+        // 存储类型筛选
+        // WebDAV 文件在上传时会在 text 字段中添加 'storage' => 'webdav' 标记
+         = '%s:7:"storage";s:6:"webdav"%';
+        if ( !== 'all') {
+            if ( === 'webdav') {
+                // 筛选 WebDAV 文件：查找 text 字段包含 webdav 存储标记的文件
+                ->where('table.contents.text LIKE BINARY ?', );
+            } elseif ( === 'local') {
+                // 筛选本地文件：排除带有 webdav 标记的文件
+                ->where('(table.contents.text IS NULL OR table.contents.text = "" OR table.contents.text NOT LIKE BINARY ?)', );
+            }
+        }
*** End Patch
